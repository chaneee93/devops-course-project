apiVersion: v1
kind: Service
metadata:
  name: course-backend
  labels:
    app: course-backend
spec:
  selector:
    app: course-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
